# Copy to .env.development (local dev) or .env.production (VPS server).
# Bun auto-loads .env.<NODE_ENV> at startup. Never commit real values.

# ---------- App ----------
NODE_ENV=development
PORT=3000
DOMAIN=localhost
API_URL=http://localhost:3000
VITE_API_URL=http://localhost:3000
NEXT_PUBLIC_API_URL=http://localhost:3000
NEXT_PUBLIC_DASHBOARD_URL=http://localhost:3002
NEXT_PUBLIC_REPO_URL=https://github.com/mg-wunna/atomic-seat
NEXT_PUBLIC_PORTFOLIO_URL=https://mg-wunna.vercel.app/
NEXT_PUBLIC_CONTACT_EMAIL=mgwunna.mw@icloud.com

# ---------- Auth ----------
# 32+ random chars. Generate: openssl rand -base64 48
JWT_SECRET=change-me-min-32-chars-use-openssl-rand-base64-48

# ---------- Database ----------
MONGO_USER=admin
MONGO_PASSWORD=password
MONGO_URI=mongodb://admin:password@localhost:27017/template?authSource=admin
SQLITE_PATH=storage/atomic-seat.sqlite

# ---------- Cache ----------
REDIS_PASSWORD=devpassword
REDIS_URL=redis://:devpassword@localhost:6379

# ---------- CORS (production) ----------
# Comma-separated list of allowed HTTPS origins. Falls back to https://www.${DOMAIN} etc. if unset.
# CORS_ORIGINS=https://www.example.com,https://app.example.com

# ---------- Storage ----------
STORAGE_PATH=./storage

# ---------- VPS only (production deploy) ----------
# ACME_EMAIL=you@example.com
# GITHUB_OWNER=your-github-username
